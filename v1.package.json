{
  "name": "beabuddhist-monorepo",
  "private": true,
  "version": "2.0.0",
  "description": "Be A Buddhist v2 monorepo — see apps/, services/, v1.package.json for legacy Ionic app",
  "scripts": {
    "build": "npm run build --prefix apps/player && npm run build --prefix apps/web",
    "dev:api": "npm run dev --prefix services/api",
    "dev:player": "npm run dev --prefix apps/player",
    "dev:web": "npm run dev --prefix apps/web"
  }
}
