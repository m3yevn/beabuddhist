import { Observable } from 'rxjs';

export class Packages{
  title:string;
  description:string;
  tasks:string;
  packageList: Observable<any>;
  currentAvatar: string;
  option:string;
  id:string;
  cat:string;
  packageDetails:any;
  }
  